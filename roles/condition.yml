- name: Print Parameter store
  hosts: localhost
  tasks:
    - name: Print a parameter
      ansible.builtin.debug:
        msg: "{{ lookup('aws_ssm', 'param.dev.catalogue.MONGO' , bypath=True , region='us-east-2') }}"